module.exports = function() {
  // If JWT secret is not present, terminate
  // if (!process.env.JWTPRIVATEKEY) {
  //   console.error('FATAL ERROR: jwtPrivateKey is not defined.');
  //   process.exit(1);
  // }
}
